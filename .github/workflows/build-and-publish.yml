name: Build and Publish Docker Images

on:
  push:
    tags:
      - 'v*'  # Trigger only on version tags like v1.0.0
  pull_request:
    branches:
      - main  # Build PRs for testing (but don't push)

env:
  REGISTRY: docker.io
  UPDATER_IMAGE_NAME: woolfg/docker-container-autoupdater-updater
  TRIGGER_IMAGE_NAME: woolfg/docker-container-autoupdater-trigger

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3

    - name: Log in to Docker Hub
      if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
      uses: docker/login-action@v3
      with:
        registry: ${{ env.REGISTRY }}
        username: ${{ secrets.DOCKER_USERNAME }}
        password: ${{ secrets.DOCKER_PASSWORD }}

    - name: Extract tag name
      if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
      id: tag
      run: echo "tag=${GITHUB_REF#refs/tags/}" >> $GITHUB_OUTPUT

    - name: Extract metadata for updater
      id: meta-updater
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.REGISTRY }}/${{ env.UPDATER_IMAGE_NAME }}
        tags: |
          type=ref,event=pr
          type=semver,pattern={{version}}
          type=raw,value=latest,enable={{is_default_branch}}
          type=raw,value={{tag}},enable={{is_default_branch}}

    - name: Extract metadata for trigger
      id: meta-trigger
      uses: docker/metadata-action@v5
      with:
        images: ${{ env.REGISTRY }}/${{ env.TRIGGER_IMAGE_NAME }}
        tags: |
          type=ref,event=pr
          type=semver,pattern={{version}}
          type=raw,value=latest,enable={{is_default_branch}}
          type=raw,value={{tag}},enable={{is_default_branch}}

    - name: Debug metadata output
      run: |
        echo "Updater tags: ${{ steps.meta-updater.outputs.tags }}"
        echo "Trigger tags: ${{ steps.meta-trigger.outputs.tags }}"
        echo "Git ref: ${{ github.ref }}"
        echo "Event name: ${{ github.event_name }}"

    - name: Build and push updater image
      uses: docker/build-push-action@v5
      with:
        context: ./updater
        platforms: linux/amd64,linux/arm64
        push: ${{ github.event_name == 'push' && startsWith(github.ref, 'refs/tags/') }}
        tags: |
          ${{ steps.meta-updater.outputs.tags }}
          ${{ github.event_name == 'push' && startsWith(github.ref, 'refs/tags/') && format('{0}/{1}:{2}', env.REGISTRY, env.UPDATER_IMAGE_NAME, steps.tag.outputs.tag) || '' }}
          ${{ github.event_name == 'push' && startsWith(github.ref, 'refs/tags/') && format('{0}/{1}:latest', env.REGISTRY, env.UPDATER_IMAGE_NAME) || '' }}
        labels: ${{ steps.meta-updater.outputs.labels }}
        cache-from: type=gha
        cache-to: type=gha,mode=max

    - name: Build and push trigger image
      uses: docker/build-push-action@v5
      with:
        context: ./trigger
        platforms: linux/amd64,linux/arm64
        push: ${{ github.event_name == 'push' && startsWith(github.ref, 'refs/tags/') }}
        tags: |
          ${{ steps.meta-trigger.outputs.tags }}
          ${{ github.event_name == 'push' && startsWith(github.ref, 'refs/tags/') && format('{0}/{1}:{2}', env.REGISTRY, env.TRIGGER_IMAGE_NAME, steps.tag.outputs.tag) || '' }}
          ${{ github.event_name == 'push' && startsWith(github.ref, 'refs/tags/') && format('{0}/{1}:latest', env.REGISTRY, env.TRIGGER_IMAGE_NAME) || '' }}
        labels: ${{ steps.meta-trigger.outputs.labels }}
        cache-from: type=gha
        cache-to: type=gha,mode=max

    - name: Display image information
      if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/')
      run: |
        echo "## Docker Images Published 🐳" >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### Updater Image" >> $GITHUB_STEP_SUMMARY
        echo '```' >> $GITHUB_STEP_SUMMARY
        echo "${{ steps.meta-updater.outputs.tags }}" >> $GITHUB_STEP_SUMMARY
        echo '```' >> $GITHUB_STEP_SUMMARY
        echo "" >> $GITHUB_STEP_SUMMARY
        echo "### Trigger Image" >> $GITHUB_STEP_SUMMARY
        echo '```' >> $GITHUB_STEP_SUMMARY
        echo "${{ steps.meta-trigger.outputs.tags }}" >> $GITHUB_STEP_SUMMARY
        echo '```' >> $GITHUB_STEP_SUMMARY
